#include <stdio.h>

int main() {
    int i;
    printf("The first 10 odd natural numbers are:\n");

    // Loop 10 times, starting from i = 1
    for (i = 1; i <= 10; i++) {
        // Print the i-th odd number (2*i - 1)
        printf("%d ", 2 * i - 1);
    }
    printf("\n");
    return 0;
}
