#include <stdio.h>

int main() {
    int n, i;

    // Ask the user to enter the limit
    printf("Enter how many odd numbers to print: ");
    scanf("%d", &n);

    printf("The first %d odd numbers are:\n", n);

    // Loop n times
    for (i = 1; i <= n; i++) {
        // Print the i-th odd number
        printf("%d ", 2 * i - 1);
    }
    printf("\n");
    return 0;
}
